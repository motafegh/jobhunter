# P2.2B Selective Responsibility Promotion Plan

**Status:** APPROVED / CONTROLLING FOCUSED PLAN — B1 REPO EVIDENCE SELECTED / `ta9l` LOCAL P1.6 AUTHORITY GATE / NO PROMOTION YET  
**Date:** 2026-09-01  
**Current-state reconciliation:** 2026-09-12  
**Scope:** First bounded P2.2B responsibility-promotion increment after P2.2A closure  
**Authority:** Subordinate to product/domain/source/architecture constraints, `docs/UTILITY_EPISTEMIC_AUTHORITY_AND_REASONING_POLICY.md`, `docs/ROADMAP.md`, `docs/IMPLEMENTATION_PLAN.md`, and `docs/P2_2_RESPONSIBILITY_WORK_ROLE_INTELLIGENCE_PLAN.md`

## 1. Decision

P2.2B is authorized only as a **selective responsibility-promotion pilot**.

The first increment must reuse the accepted Canonical Registry contract unchanged unless a concrete eligible responsibility correspondence proves that the current contract cannot represent it safely.

Current B1 flow:

```text
accepted/current P1.6 responsibilities
→ bounded repository evidence selection COMPLETE
→ ta9l selected as the single additional evidence-bearing job
→ ta9l current English projection / P1.6 authority gate NEXT
→ semantic review of the exact accepted cross-job correspondence
→ only if non-lossy and reusable: one reviewed responsibility concept + exact claim mappings
→ CLI/browser/currentness/idempotency verification
→ B1 PASS or evidence-based NO-PROMOTION / DEFER
```

Do not start P2.2C responsibility families until this pilot demonstrates useful reusable responsibility authority or explicitly records that current evidence is insufficient.

## 2. Important rejected first candidate

The initially considered pair is **not approved for promotion**:

`t4qV` P1.6 artifact 44 responsibility[9]:

> Preparing technical documentation for security equipment such as Cisco Secure Firewall ...

`tmyX` P1.6 artifact 46 responsibility[3]:

> Preparing checklists, technical documentation, and security reports

Reason:

- `t4qV` is specifically technical documentation for security equipment;
- `tmyX` is compound work covering checklists, technical documentation, and security reports;
- the current registry maps one immutable P1.6 responsibility claim to one canonical responsibility concept;
- forcing both claims into a single broad documentation concept could erase source distinctions or silently treat a partial overlap as whole-claim correspondence.

This is exactly the kind of normalization loss P2.2B must avoid.

Additional rejected/secondary evidence is recorded in `docs/EXECUTION_TODO.md` and `docs/WORKING_MEMORY.md`. Do not reopen those alternatives merely to avoid testing the selected candidate.

## 3. B1 objective

Prove one **genuinely reusable, non-lossy responsibility correspondence** using the existing Canonical Registry.

A successful B1 should normally produce only:

```text
1 reviewed canonical responsibility concept
2 exact accepted/current P1.6 responsibility mappings from different jobs
0 automatic aliases unless independently justified
0 deliverable promotions
```

This is a bounded proof, not a completeness target.

A valid B1 result may also be **NO-PROMOTION / DEFER** if the selected evidence does not survive the P1.6 or final correspondence gate.

## 4. Eligibility rule

A responsibility pair is eligible only when all of the following hold:

1. both inputs are accepted/current English P1.6 `responsibilities[]` claims;
2. they express the same reusable responsibility identity at the canonical level;
3. differences are harmless wording, grammar, specificity that can remain source detail, or equivalent surface formulation;
4. the mapping does not discard an additional material action, object, ownership relationship, lifecycle endpoint, or compound duty from either source claim;
5. the normalized concept has a concrete downstream reuse hypothesis, primarily P2.2C family membership or later capability-profile reuse;
6. exact source wording and provenance remain recoverable from each mapping.

Do not promote merely because two statements share keywords or belong to the same broad theme.

## 5. Bounded evidence selection — repository side complete

The bounded evidence search authorized by the original plan is complete. Do **not** repeat the scan before the selected candidate is tested.

Selected additional evidence-bearing job:

```text
ta9l — Senior Applied AI Engineer — Mofid Securities
source detail version: 25
source state: parsed / active
current committed English projection: none
current accepted P1.6: none
semantic sha256: cd9dbf6be622113836b951e9042c87798954fc01dea520ba08b090eae2b54fc6
```

Accepted anchor:

```text
tG9K P1.6 artifact 36 responsibility[5]
Design rigorous validation and monitoring for models running in an industrial setting.
```

Selected `ta9l` source duty:

```text
Create evaluation, testing, and observability frameworks for LLM and agent performance.
```

Tentative identity — not yet promoted:

```text
responsibility:design-ai-evaluation-monitoring
preferred label: Design AI evaluation and monitoring
```

Current semantic hypothesis:

```text
design/create
→ AI-system performance evaluation/validation
→ monitoring/observability
```

Source-specific distinctions must remain recoverable:

```text
tG9K: industrial-setting models; rigorous validation + monitoring
ta9l: LLMs and agents; evaluation + testing + observability frameworks
```

No ownership, deployment, or lifecycle strengthening is authorized.

The repository-side preflight also confirmed that `ta9l` has no current committed English projection. The exact local execution packet is:

```text
docs/working-memory/2026-09-06_P2_2B_B1_TA9L_LOCAL_RUNTIME_PREFLIGHT.md
```

Do not select a second additional job unless `ta9l` is explicitly rejected and a new focused decision authorizes continued evidence search.

## 6. Existing-registry reuse

Use the existing contract:

```text
jobhunter-canonical-concept-registry-v1
```

Expected existing capabilities:

- `ConceptCategory.RESPONSIBILITY`;
- reviewed stable `responsibility:<slug>` IDs;
- exact accepted-P1.6 responsibility mapping provenance;
- immutable mapping decisions;
- currentness/stale-dependency behavior;
- idempotent reviewed mutations;
- CLI and browser inspection.

Do not create a parallel responsibility ontology or a P2.2-specific canonical store.

## 7. Deliverables are deferred from B1

The registry has a `deliverable` concept category, but the current reviewed claim-mapping identity is built around P1.6 `requirement | responsibility` claims and does not provide a reviewed mapping identity for P2.2A Work Intelligence deliverables.

Do not add deliverable schema/mapping machinery speculatively.

Reopen deliverable promotion only after a concrete repeated deliverable demonstrates downstream value and gives us an evidence-backed mapping identity to design around.

## 8. `ta9l` P1.6 authority gate

The selected `ta9l` source duty is **not eligible for mapping yet** because it lacks accepted/current P1.6 authority.

Before any registry mutation:

1. run local doctor / translation-provider status against the real owner runtime;
2. create or reuse a current English projection for `ta9l`;
3. inspect that projection;
4. generate English P1.6 using `job-analysis-english-v20 / job-analysis-v5`;
5. inspect the complete candidate and its semantic-review status;
6. semantically accept or reject the artifact on its whole quality, not merely the selected responsibility;
7. if accepted, record exact artifact ID, responsibility index, statement, evidence, and review state;
8. compare that **exact accepted P1.6 claim** with `tG9K` P1.6 36 responsibility[5];
9. stop before mutation if the P1.6 output materially splits, strengthens, weakens, merges, or otherwise changes the selected source-duty shape.

A useful selected claim does not justify accepting an otherwise defective P1.6 artifact.

## 9. Promotion operation if the accepted pair remains eligible

Before mutation, record the exact candidate:

```text
job A / P1.6 artifact / responsibility[index] / exact statement
job B / P1.6 artifact / responsibility[index] / exact statement
proposed responsibility:<slug>
preferred label
definition or boundary note if needed
why the correspondence is non-lossy
what downstream reuse it enables
```

Then:

1. create/reuse exactly one reviewed responsibility concept;
2. map the two exact accepted/current P1.6 responsibility claims;
3. rerun the same reviewed operations to prove idempotent reuse;
4. inspect current mappings in CLI;
5. inspect the concept and mappings in browser;
6. verify stale/current behavior remains unchanged;
7. verify no public-corpus mutation/publication occurred.

No LLM generation is required for the promotion mutation itself.

## 10. Stop / success rules

### Success

B1 succeeds when the selected responsibility identity is proven reusable across the two accepted/current jobs without material semantic loss, and the existing registry safely persists and exposes that reviewed authority.

### No-promotion / defer result

If `ta9l` is rejected at the P1.6 gate or the final exact claim is not a safe non-lossy correspondence:

```text
record the evidence-based failure/insufficiency
→ create no canonical responsibility merely to make B1 look complete
→ create no mappings for a partial/compound overlap
→ decide B1 NO-PROMOTION / DEFER unless a new focused decision explicitly authorizes another candidate search
```

A no-promotion result is valid evidence, not a project failure.

## 11. Non-goals / stop lines

During B1:

- do not repeat the already-completed broad/bounded repository scan merely to find an easier pair;
- do not bulk-map accepted responsibilities;
- do not normalize partial semantic overlaps as whole-claim equivalence;
- do not broaden a concept merely to absorb compound claims;
- do not auto-promote P2.2A theme or deliverable labels;
- do not add deterministic verb-equivalence machinery;
- do not add responsibility families or stable role archetypes yet;
- do not publish registry or P2.2 state;
- do not start the formal Market / Role-Family foundation investigation while B1 is open;
- do not implement Market-v2, personal intelligence, scoring, or recommendations;
- do not add schema/migrations unless the concrete eligible pair exposes an actual contract gap.

The already-completed Market research/preparation work does not violate this stop line; it remains design input only and is recorded separately.

## 12. Exact next action

Before any local execution, use the recorded preflight packet rather than repeating repository evidence selection:

```text
local doctor / translation-provider status
→ ta9l current English projection
→ inspect current English projection
→ ta9l English P1.6 v20/v5 generation
→ complete semantic review / accept or reject
→ if accepted, report exact artifact ID + responsibility index + statement + evidence
→ final correspondence review against tG9K P1.6 36 responsibility[5]
→ only if still non-lossy: one concept + two mappings
→ idempotency + CLI/browser/currentness/publication-boundary checks
→ B1 PASS or evidence-based NO-PROMOTION / DEFER
```

After B1 closes, follow the current-state reconciliation and queued Market investigation route. Do not start P2.2C or Market implementation merely because B1 execution has ended.