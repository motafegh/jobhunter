# Market I7 — v23 complete non-persistent case review

**Date:** 2026-09-26
**Status:** BOUNDED CASE SEMANTIC REVIEW PASS / PUBLIC PROMOTION PENDING / I7 HOLD
**Input:** unchanged current `tvMm` source detail 47 and English projection 41.
**Evaluation head:** `592e5d0`.

The owner explicitly requested continuing the I7 repair after the previous validator-only
checkpoint. The corrected v23 value-preference guard was already locally gated at 734
warnings-as-errors tests, Ruff PASS, and public corpus verification PASS for 394 jobs.
One further non-persistent call used the configured `gemma-4-e4b-it-ud` model and the same
source projection. It returned a mechanically valid complete result in 183 seconds: 25
requirements, 11 responsibilities, no role-purpose claims, and no coverage exclusions.
The ignored private protocol is at
`data/local-acceptance/i7/v23-value-guard-evaluation-20260926.json`.

## Whole-result source review

- The five required technical groups preserve practical LLM/API experience,
  Agent/Workflow building experience, and three separate familiarity scopes for
  Tool Calling, RAG/Embedding/Vector Databases, and one-or-more Agentic Frameworks.
  Python/TypeScript and framework alternatives remain alternatives.
- The architecture-knowledge item and real-system-building-experience item remain
  separate. Seven preferred technical/work clauses retain preferred strength;
  familiarity is not borrowed across neighboring Docker/Cloud or work items.
- The reliable-product Agent clause is described as an ability to turn an Agent
  into a reliable system, with no fabricated prior-delivery experience.
  Headingless prior Agent/API/LLM experience and enjoyment of building AI systems
  retain their distinct source subjects.
- All three explicit proof preferences are present as preferred, nontechnical
  candidate evidence: show and explain a previously built Agent with its source
  follow-up questions; a real GitHub repository, sample project, or demo; and a
  self-built real project valued over a longer resume. The original mixed-language
  source independently contains the large-plus and value wording. The separate
  application submission instruction is not represented as a qualification.
- The two exact structured source skill tags are retained. All eleven source work
  statements are covered, including cost/latency/reliability/quality and Product/
  Backend collaboration. Company goals are not turned into candidate duties.
- No requirement asserts stronger depth than its exact item. No duty is inferred
  from candidate qualifications. Evidence and full-result validation passed the
  versioned v23 source, coverage, subject, and ontology guards.

The short concept `Provision of project samples` is less specific than its cited
GitHub/project/demo alternatives, but its exact source evidence retains them; it does
not impose a particular tool or multiple submissions. This is a bounded wording
limitation, not a material source error in this case.

## Authority and state

This is one reviewed **non-persistent candidate result**, not general model accuracy
or a durable accepted artifact. The operational SQLite and all public corpus JSON
files remained byte-identical before and after; SQLite integrity was `ok`, with zero
foreign-key violations. No analysis artifact, review decision, Market snapshot,
profile, or corpus projection was created by the evaluation.

Next: reconcile the v23 public/current route while preserving accepted-only v20
compatibility and all historical version identities; pass focused and full gates;
run one normal persisted `tvMm` analysis; explicitly review its actual artifact;
accept only if valid, then complete the new Market snapshot/profile and shared
CLI/browser semantic drill-down. I7 remains HOLD until those steps succeed.
