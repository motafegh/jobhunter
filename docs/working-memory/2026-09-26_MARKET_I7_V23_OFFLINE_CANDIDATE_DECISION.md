# Market I7 — isolated v23 proof and claim-wording candidate

**Date:** 2026-09-26
**Status:** BOUNDED EVALUATION CLOSED / NO CANDIDATE / I7 HOLD
**Parent:** `2026-09-24_P16_V22_POST_ALIAS_EVALUATION_CLOSURE.md` and `2026-09-26_P16_R03_R04_OFFLINE_SOURCE_AND_CONSUMER_AUDIT.md`.

## Source-backed problem

The one v22 `tvMm` evaluation preserved most required and preferred technical facts but displayed a required ability to make an Agent reliable in a product as prior `experience`. It also omitted the explicitly preferred demonstration of a previously built Agent and repository/project/demo evidence. Those failures are distinct: incorrect claim wording versus missing source coverage. The v22 guard rejects type-only abstention for the first; the v21 planner never supplied the second as requirement coverage.

Across the 27 current English projections, `tvMm` has an explicit huge plus, valuable alternatives, and a real project valued above a resume. `taOX` has a work-sample submission with significant resume-review impact, already covered by v21 as preferred. `tI1n`, `takb`, `tm4I`, and `tGc5` provide submission-only or mixed qualification/submission negatives. `tjgi` lists at least one real-world sample among candidate qualifications; it is a distinct uncovered required-strength case, not evidence for a preferred proof item. These are derived English projections; original source remains authoritative.

## Candidate boundary

`job-analysis-english-v23` remains isolated and keeps the v21 exact item/parent evidence and v22 fail-closed ontology response model. A conservative planner adds exact, source-present references only where proof-of-work language and explicit preference language coexist, while excluding application directives. The added references are separately partitioned, preferred, non-excludable, and carry exact item excerpts. No concept, count, or employer fact is deterministically synthesized from a keyword match. The v23 prompt asks the model to name ability-only concepts without invented prior history and to keep proof preferences separate from technical requirements and submission instructions. Whole-result semantic review remains mandatory.

The five accepted anchor plans are identical under v21 and v23. The 27-projection offline audit checks source containment and duplicate-free proof references. The current public route stays v21/v5, accepted v20/v5 anchors keep their exact identities, and v22/v23 are not accepted Market inputs.

Offline gate on this head: Ruff passed, the full warnings-as-errors suite passed **731 tests**, and public-corpus verification passed for all **394 known jobs**.

## Evaluation decision boundary

After Ruff, full strict-warning tests, public corpus verification, and source/plan review pass, authorize **one non-persistent v23 evaluation on the unchanged `tvMm` projection using the configured analysis model and existing bounds**. Record requests and raw responses only in ignored local evidence, compare operational SQLite and corpus before/after, and review every generated claim, exclusion, obligation, depth, duty, and proof preference against the source. A validated model response remains a candidate, not semantic acceptance.

If that bounded result fails materially, preserve its exact failure and stop. Do not repeat the same call, switch model or vacancy, relax validation, hand-assemble a candidate, promote v23, or change Market snapshots to manufacture I7 PASS. If it passes complete semantic review, separately reconcile public routing/currentness and run one normal persisted v23 candidate path, followed by explicit artifact review and only then the remaining I7 snapshot/profile and browser/CLI drill-down checks.

## Preflight interruption and exact-span repair

The first v23 non-persistent call was interrupted during its first HTTP response wait after 42 seconds, before any model result or analysis artifact existed. Source review during that wait found a general planner defect: sentence splitting ended an explicit preferred demonstration request at its first question and omitted immediately following questions about the work. The ignored private record reports unchanged SQLite/corpus hashes, `integrity=ok`, and zero foreign-key violations. It is an interrupted preflight, not an evaluated semantic result or authorization to accept anything.

The candidate planner now retains the exact contiguous source span through adjacent follow-up questions, preserving whitespace and the full `tvMm` demonstration context. A regression checks each question and a separate newline-preserving example. This repairs source evidence before the first completed v23 evaluation; the next check must use a distinct ignored record and re-run offline gates first.

## Completed bounded evaluation and closure

At pushed head `11ad2f7`, the single completed non-persistent v23 `tvMm` call ran with the configured model and one bounded validation retry. Both responses included the three explicit proof preferences, but both failed the inherited v21 preferred-item validator on the two exact statements containing `very valuable` and `more valuable`. It recognized `plus` but not these explicit source preference phrases. Instructor returned `InferenceResponseError`; no validated candidate, analysis artifact, review action, public corpus export, or Market state was created. Private raw protocol and the failure record remain ignored at `data/local-acceptance/i7/v23-post-span-evaluation-20260926.json`. The runner verified byte-identical operational SQLite and corpus content before/after, SQLite integrity `ok`, and zero foreign-key violations.

The owning deterministic gap is repaired only for v23 `candidate_proof` references: exact `very valuable` or `more valuable` now satisfies the preferred-item check when the corresponding source-backed proof parent is marked preferred. The public v21 rule remains behaviorally unchanged. Read-only replay of each retained three-requirement proof response now validates under the repaired v23 model; this is structural replay of one partition, not a completed whole-analysis candidate or semantic acceptance. A non-proof parent with the same words still fails closed.

The offline comparison also corrected the `tjgi` strength error. `Having at least one real-world sample` appears in candidate qualifications and does not say preferred. V23 no longer creates a preferred proof ref for it. Its required-strength coverage remains an open general question and is not silently counted as met. The five accepted anchor plans and public/current v21/v5 contract remain unchanged.

Post-repair offline gates: Ruff PASS, full warnings-as-errors suite **734 passed**, and public-corpus verification PASS for all **394 known jobs**.

This completed model invocation is closed. Do not repeat `tvMm`, switch model or vacancy, persist replayed fragments, or promote v23 from structural replay. I7 remains HOLD because there is still no semantically accepted-current core P1.6 artifact or new snapshot/profile and CLI/browser semantic drill-down. Further work needs independent representative evidence for the required proof-sample boundary or a naturally available valid accepted-current core artifact, followed by a separate explicit versioned decision.
