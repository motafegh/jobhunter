# P1.6 v23 current-contract promotion

**Date:** 2026-09-26
**Status:** REPOSITORY PROMOTION / I7 STILL HOLD

The owner directed the I7 repair to continue through the corrected model path.
The complete non-persistent v23 `tvMm` result was structurally valid and passed
manual review of all 25 requirements, 11 duties, exclusions, source evidence,
strength, depth, subject and original-source proof preferences. Its review is in
`2026-09-26_MARKET_I7_V23_FULL_EVALUATION_REVIEW.md`.

V23/v5 is now the public/current English P1.6 generation and review contract.
The original-language v9/v4 path is unchanged. Accepted v20/v5 anchors retain
their physical identity and remain compatibility-current; accepted v21/v5
artifacts are also compatible. Pending or rejected prior versions cannot
satisfy v23 currentness. No historical artifact is relabeled or regenerated
for this promotion.

The shared AnalysisStore and browser queries implement the same compatibility
rule. Normal CLI/browser routing and item review now use v23 for new English
candidates. The corpus manifest advertises v23; its five accepted v20 anchors
remain exact source-backed public artifacts. Market still has no accepted-current
semantic core member from this promotion alone.

Repository gates at promotion: Ruff PASS, full warnings-as-errors suite **738
passed**, public corpus export/verification PASS for **394 known jobs**, and
source diff checks PASS. Dedicated regressions show accepted v20 and v21 reuse
with exact prompt identity and exclusion of pending v20/v21 candidates.

Next real gate: normal persisted v23 `tvMm` analysis, actual artifact review and
explicit acceptance only if it is source-valid, then new immutable Market
snapshot/profile with accepted-semantic requirement/responsibility drill-down
in CLI and browser, followed by operational integrity and privacy checks.

## Persisted path repair before the next run

The first normal CLI `tvMm` analysis under v23 recorded failed attempt **110** and
no artifact. Its model result reached the service, but the inherited persistence
function rebuilt the much older v14/base coverage plan and demanded reference
`field:description:segment:2:clause:1`, which the v23 model-facing planner had
already replaced with exact item references. This was a real integration defect,
not semantic rejection. Pre-run SQLite backup is ignored locally; integrity was
`ok` with zero foreign-key violations.

V23 now persists coverage from its own exact requirement planner and the inherited
v21 responsibility planner. Every mandatory ref must be cited; prohibited proof
exclusions fail closed; structured skill tags retain deterministic coverage;
work refs must be purpose or responsibility. The historical v20 persistence path
is unchanged. The current service now also configures private failure diagnostics
as v21 did. Read-only replay of the reviewed full 25-requirement/11-duty result
through both v23 persistence and v17 source validation passes. A separate public
source regression constructs the full `tvMm` ledger and rejects missing or
excluded proof. Ruff PASS, **740 warnings-as-errors tests PASS**, public corpus
verification PASS for 394 jobs. Attempt 110 remains failed and is not promoted.
