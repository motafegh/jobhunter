# Working Memory — P1.6 v13 Sparse CI-3 Handoff

**Date:** 2026-08-13  
**Status:** Active non-authoritative handoff  
**Current gate:** CI-3 heterogeneous semantic validation  
**Active sparse job:** `t4jp`

## Current accepted/public baseline

```text
English P1.6 public: job-analysis-english-v9 / job-analysis-v4
Capability accepted: job-capability-intelligence-v7 / job-capability-intelligence-v4
Blueprint: experimental/non-gating
```

`tG9K` remains the accepted dense anchor on P1.6 artifact `29` and Capability artifact `9`.

## Sparse `t4jp` experiment history

```text
v9 artifact 30
→ rejected: structured skills could disappear; qualification became responsibility

v10 artifact 31
→ 0 responsibilities / 7 requirements / 3 structured skills
→ mechanical PASS, semantic FAIL because coarse paragraph coverage lost distinct qualifications

v11
→ failed before persistence: deterministic qualification spans were raw strings rather than normal evidence-reference IDs

v12
→ evidence-reference problem fixed
→ live generation produced all 4 granular qualification items + all 3 structured skills + 0 responsibilities
→ Instructor corrected invalid `ability to ...` depth_signal to null
→ still failed before persistence because coarse `field:description:segment:0` remained a model-owned coverage obligation

v13
→ active isolated candidate
```

## v13 correction

Principle:

```text
model owns semantic claims
JobHunter owns deterministic provenance/bookkeeping
```

v13 candidate-only Instructor behavior:

- exact qualification items remain first-class evidence references;
- JobHunter detects coarse requirement coverage containing multiple exact qualification items;
- only references already marked `allow_exclusion=True` may be suppressed from the model/Pydantic coverage checklist;
- unknown/non-excludable references fail closed;
- structured requirements such as education cannot be suppressed;
- after semantic generation, JobHunter injects deterministic decomposition bookkeeping;
- persistence records the coarse span as `decomposed_requirement`.

Production/public v9 and the shared production Instructor path remain unchanged.

## v13 implementation

```text
src/jobhunter/analysis_service_v13.py
src/jobhunter/analysis_runtime_v13.py
src/jobhunter/inference/instructor_lm_studio_v13.py
src/jobhunter/p16_v13_snapshot.py
src/jobhunter/p16_v13_audit.py
scripts/run_p16_v13_candidate.py
scripts/export_p16_v13_candidate_snapshot.py
scripts/audit_p16_v13_candidate_snapshot.py
tests/test_analysis_v13_candidate.py
```

Implementation CI on commit `e11ed0f37af508a780c07ab06668adab276586ef`:

```text
Ruff:               PASS
full pytest:        PASS
warnings-as-errors: PASS
```

Decision record:

```text
docs/experiments/2026-08-13_P16_V12_COARSE_COVERAGE_FAILURE_AND_V13_DETERMINISTIC_DECOMPOSITION.md
```

## Exact next step

Do not run Capability and do not run dense `tG9K` yet.

Run sparse v13 only:

```bash
git pull --ff-only origin main
python scripts/run_p16_v13_candidate.py --job-id t4jp
```

If generation completes:

```bash
python scripts/export_p16_v13_candidate_snapshot.py --job-id t4jp
python scripts/audit_p16_v13_candidate_snapshot.py --job-id t4jp
git status --short
```

Mechanical target:

```text
3/3 structured required skills
4/4 qualification-list items
0 qualification-derived responsibilities
>=1 decomposed_requirement coverage decision
```

If mechanical audit passes, commit/push the snapshot and perform full semantic review before dense regression.

Only after sparse semantic PASS:

```text
run v13 on tG9K
→ compare against accepted P1.6 artifact 29
→ if no material dense regression, decide v13 promotion
→ only then rebuild/review Capability v7 on promoted P1.6 identity
```
