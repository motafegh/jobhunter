# P1.6 v16 Handoff

**Date:** 2026-08-14  
**Gate:** sparse CI-3 on `t4jp`

v15 artifact `34` passed mechanical audit but failed semantic review. Its only remaining bad requirement was persisted as:

```text
concept: Visual content production ( )
concept_type: experience
evidence: ability to produce visual content full-time and part-time
```

The source supports an ability, not prior applied exposure, and normalized concepts must not retain empty punctuation debris. Because v15 artifact 34 exists, v15 is frozen and not promotable.

Active candidate is now:

```text
job-analysis-english-v16 / job-analysis-v4
```

v16 preserves v15 coverage/decomposition/strength/depth behavior and adds two generic guards:

- normalized concepts cannot contain empty grouping punctuation left by cleanup;
- `concept_type=experience` requires source evidence of prior applied exposure; `ability to ...` alone is insufficient.

Deterministic code cleans punctuation only. It does not silently retag semantic concept types; the model receives one bounded correction when the type guard fails.

Implementation head `e14bb3fe27aa5af13704156b1638085185a4ed1a` passed CI run 701: Ruff, full pytest, and warnings-as-errors all PASS.

Next: run sparse v16 only. Do not run Capability or dense `tG9K` until sparse v16 passes semantic review.
