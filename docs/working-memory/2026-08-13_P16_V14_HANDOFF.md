# Working Memory — P1.6 v14 Sparse CI-3 Handoff

**Date:** 2026-08-13  
**Status:** Active non-authoritative handoff  
**Current gate:** CI-3 heterogeneous semantic validation  
**Active sparse job:** `t4jp`

## Accepted/public baseline

```text
English P1.6 public: job-analysis-english-v9 / job-analysis-v4
Capability accepted: job-capability-intelligence-v7 / job-capability-intelligence-v4
Blueprint: experimental/non-gating
```

`tG9K` remains the accepted dense anchor on P1.6 artifact 29 and Capability artifact 9.

## Sparse history

```text
v9 artifact 30
→ rejected: structured skills could disappear; qualification became responsibility

v10 artifact 31
→ mechanical PASS; semantic FAIL because coarse coverage lost explicit list qualifications

v11
→ failed before persistence: candidate qualification spans were not normal evidence IDs

v12
→ first-class evidence IDs fixed recall
→ failed before persistence because coarse coverage remained model-owned bookkeeping

v13 artifact 32
→ 0 responsibilities / 7 requirements
→ 3/3 structured skills + 4/4 qualification items
→ mechanical structure good
→ semantic FAIL:
   1. coarse suppression hid explicit Ethics/work commitment expectation
   2. visual-content concept retained Ability-to + full-time/part-time schedule wording

v14 first live run
→ failed closed before persistence only at depth validation
→ model otherwise produced the intended sparse semantic shape:
   - 0 responsibilities
   - 8 requirements
   - Ethics/work commitment preserved
   - teachability excluded
   - remote-work instruction excluded
   - location/benefits excluded
   - visual-content concept normalized to Producing visual content
→ remaining defect: model put `full-time and part-time` into `depth_signal`
```

No v15 was created because no v14 artifact was persisted and the v14 semantic contract did not
change. The failure was an implementation-ordering boundary: schedule wording reached the shared
depth validator before v14 could normalize it.

## v14 principle

```text
complete decomposition:
coarse span
→ mandatory exact qualification items
+ exact residual sentences
→ every residual must be extracted or explicitly excluded
```

This prevents deterministic decomposition from creating a new recall hole.

For `t4jp`, expected residual sentences are:

```text
the work is teachable.
Ethics and your work commitment are important to us.
Please do not send your resume for remote work.
(Location: West Tehran) Benefits include ...
```

Expected semantic result:

- teachability: excluded;
- Ethics/work commitment: extracted as employer expectation;
- remote-work application instruction: excluded;
- location/benefits: excluded.

Capability concepts must not keep `Ability to ...` wrappers or `full-time` / `part-time` schedule
wording. Exact evidence remains unchanged.

## Schedule vs depth correction

`full-time` / `part-time` are work-arrangement signals, not capability-depth or experience-extent
signals. v14 now applies a candidate-only typed normalization before the shared P1.6 depth
validator:

```text
schedule-only depth_signal
→ null

accepted depth signal present
→ preserve it for the shared validator
```

Examples:

```text
full-time and part-time
→ null

expert visual content production full-time
→ shared validator preserves expert
```

The shared public depth validator remains strict and unchanged. Historical v13 source was restored
unchanged after the implementation correction. The persisted v14 snapshot audit also rejects any
`depth_signal` containing full-time/part-time schedule wording.

## v14 implementation

```text
src/jobhunter/analysis_service_v14.py
src/jobhunter/analysis_runtime_v14.py
src/jobhunter/inference/instructor_lm_studio_v14.py
src/jobhunter/p16_v14_snapshot.py
src/jobhunter/p16_v14_audit.py
scripts/run_p16_v14_candidate.py
scripts/export_p16_v14_candidate_snapshot.py
scripts/audit_p16_v14_candidate_snapshot.py
tests/test_analysis_v14_candidate.py
```

## Repository verification

The schedule/depth correction passed the repository quality gate:

```text
Ruff:               PASS
full pytest:        PASS
warnings-as-errors: PASS
```

Regression coverage includes:

- schedule-only depth becomes null before shared validation;
- genuine accepted depth survives nearby schedule wording;
- persisted v14 artifacts cannot contain schedule wording in `depth_signal`;
- complete qualification/residual coverage behavior remains tested.

Experiment record:

```text
docs/experiments/2026-08-13_P16_V13_SEMANTIC_FAILURE_AND_V14_COMPLETE_DECOMPOSITION.md
```

## Exact next step

Do not run Capability and do not run dense `tG9K` yet.

Synchronize and rerun sparse v14 only:

```bash
git pull --ff-only origin main
python scripts/run_p16_v14_candidate.py --job-id t4jp
```

If completed:

```bash
python scripts/export_p16_v14_candidate_snapshot.py --job-id t4jp
python scripts/audit_p16_v14_candidate_snapshot.py --job-id t4jp
git status --short
```

Mechanical target:

```text
3/3 structured skills
4/4 qualification-list items
complete residual coverage decisions
>=1 decomposed_requirement
0 qualification-derived responsibilities
no Ability-to/full-time/part-time capability concept
no full-time/part-time depth_signal
```

If mechanical PASS, commit/push the snapshot and perform full semantic review.

Only after sparse semantic PASS:

```text
run v14 on tG9K
→ compare against accepted v9 artifact 29
→ if no material dense regression, decide v14 promotion
→ then rebuild/review Capability v7 on the promoted P1.6 identity
```
